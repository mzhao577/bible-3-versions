__pycache__/
*.pyc
.venv/
.DS_Store
scripts/_cache/

# Never commit a locally-imported copyrighted version.
recovery*
恢复本*
*.bbl.mybible
*.bblx

# Local tooling / scratch, not part of the app.
.claude/
instruction.txt

# Private database holding a licensed version (e.g. the Recovery Version).
# The committed data/bible.sqlite must stay public-domain-only.
data/bible.local.sqlite
biblerain_*
*.epub
